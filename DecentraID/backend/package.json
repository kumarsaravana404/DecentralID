{
  "name": "decentraid-backend",
  "version": "1.0.0",
  "description": "DecentraID Backend Services - Production Ready",
  "main": "server.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "cross-env NODE_ENV=production node server.js",
    "start:prod": "cross-env NODE_ENV=production node server.js",
    "dev": "cross-env NODE_ENV=development nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "test:supabase": "node test-supabase.js",
    "lint": "eslint .",
    "health": "curl -f http://localhost:${PORT:-5000}/health || exit 1",
    "check-deploy": "node check-deployment.js"
  },
  "keywords": [
    "blockchain",
    "identity",
    "ssi",
    "decentralized"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@pinata/sdk": "^2.1.0",
    "@supabase/supabase-js": "^2.95.3",
    "compression": "^1.8.1",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "ethers": "^6.16.0",
    "express": "^5.2.1",
    "express-rate-limit": "^7.1.5",
    "express-validator": "^7.0.1",
    "helmet": "^7.1.0",
    "mongoose": "^8.1.0",
    "multer": "^2.0.2",
    "winston": "^3.11.0"
  },
  "devDependencies": {
    "cross-env": "^10.1.0",
    "nodemon": "^3.0.3"
  }
}
